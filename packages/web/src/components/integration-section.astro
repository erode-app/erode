<section class="integration">
  <div class="container">
    <h2>Get Started</h2>
    <p class="subtitle">Add erode to your CI pipeline in minutes.</p>
    <div class="code-grid">
      <div class="code-block">
        <div class="code-header">
          <span class="code-lang">GitHub Actions</span>
        </div>
        <pre><code><span class="comment"># .github/workflows/erode.yml</span>
name: Architecture Drift Check
on: [pull_request]

jobs:
  erode:
    runs-on: ubuntu-latest
    steps:
      - uses: erode-app/action@v1
        with:
          model-path: ./architecture
        env:
          GITHUB_TOKEN: $&#123;&#123; secrets.GITHUB_TOKEN &#125;&#125;
          AI_PROVIDER: gemini
          GEMINI_API_KEY: $&#123;&#123; secrets.GEMINI_API_KEY &#125;&#125;</code></pre>
      </div>
      <div class="code-block">
        <div class="code-header">
          <span class="code-lang">CLI</span>
        </div>
        <pre><code><span class="comment"># Install</span>
npm install -g erode

<span class="comment"># Analyze a pull request</span>
export GITHUB_TOKEN="ghp_..."
export GEMINI_API_KEY="AIza..."
erode analyze --pr 42 --repo owner/repo

<span class="comment"># With Anthropic instead</span>
export AI_PROVIDER=anthropic
export ANTHROPIC_API_KEY="sk-ant-..."
erode analyze --pr 42 --repo owner/repo</code></pre>
      </div>
    </div>
  </div>
</section>

<style>
  .integration {
    padding-block: var(--space-2xl);
    border-top: 1px solid var(--border);
  }

  h2 {
    text-align: center;
  }

  .subtitle {
    max-width: 28rem;
    margin: var(--space-md) auto 0;
    color: var(--text-muted);
    text-align: center;
  }

  .code-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-lg);
    margin-top: var(--space-xl);
  }

  .code-block {
    border: 1px solid var(--border);
    border-radius: var(--radius-lg);
    overflow: hidden;
    background: var(--bg-secondary);
  }

  .code-header {
    padding: var(--space-sm) var(--space-md);
    background: var(--bg-elevated);
    border-bottom: 1px solid var(--border);
  }

  .code-lang {
    font-family: var(--font-mono);
    font-size: 0.75rem;
    color: var(--text-tertiary);
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  pre {
    padding: var(--space-md) var(--space-lg);
    overflow-x: auto;
    margin: 0;
  }

  code {
    font-family: var(--font-mono);
    font-size: 0.8rem;
    line-height: 1.7;
    color: var(--text-muted);
  }

  .comment {
    color: var(--text-tertiary);
  }

  @media (max-width: 768px) {
    .code-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
