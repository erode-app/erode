<section class="container">
  <h2>Features</h2>
  <div class="grid">
    <div class="feature">
      <span class="feature-icon">🤖</span>
      <h3>Multi-Provider AI</h3>
      <p>
        Choose between Gemini, OpenAI, and Anthropic (experimental). Cheaper
        models for extraction, stronger models for analysis.
      </p>
    </div>
    <div class="feature">
      <span class="feature-icon">⚡</span>
      <h3>CI Integration</h3>
      <p>
        Runs as a GitHub Action with PR commenting and configurable review
        thresholds. GitLab and Bitbucket support experimental.
      </p>
    </div>
    <div class="feature">
      <span class="feature-icon">🔀</span>
      <h3>Auto-Fix PRs <span class="badge">Coming soon</span></h3>
      <p>
        Optionally generates a change request with LikeC4 or Structurizr DSL
        updates to fix detected drift.
      </p>
    </div>
    <div class="feature">
      <span class="feature-icon">🚨</span>
      <h3>Finding Severity</h3>
      <p>
        Categorizes findings as high, medium, or low severity, each with a
        concrete suggestion for how to address or document the change.
      </p>
    </div>
    <div class="feature">
      <span class="feature-icon">📦</span>
      <h3>Monorepo Aware</h3>
      <p>
        Resolves the correct architecture component when a single repo maps to
        multiple components.
      </p>
    </div>
    <div class="feature">
      <span class="feature-icon">🏗️</span>
      <h3>Architecture-as-Code</h3>
      <ul class="format-list">
        <li>
          <a href="https://likec4.dev" class="format-link">LikeC4</a>
          <span class="format-desc">
            &mdash; recommended &middot;
            <a href="https://github.com/erode-app/playground/pull/1">See example</a>
          </span>
        </li>
        <li>
          <a href="https://docs.structurizr.com" class="format-link">Structurizr</a>
          <span class="badge">Experimental</span>
          <span class="format-desc">
            &middot;
            <a href="https://github.com/erode-app/playground/pull/2">See example</a>
          </span>
        </li>
      </ul>
    </div>
  </div>
  <a href="/docs/getting-started/" class="docs-link">Explore the docs &rarr;</a>
</section>

<style>
  section {
    padding-block: var(--space-2xl);
    border-top: 1px solid var(--border);
  }

  h2 {
    text-align: center;
    color: var(--text-primary);
  }

  .grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--space-lg);
    margin-top: var(--space-xl);
  }

  .feature {
    padding: var(--space-lg);
    background: var(--bg-secondary);
    border: 1px solid var(--border);
    border-radius: var(--radius-lg);
    transition: border-color 0.2s, box-shadow 0.2s;
  }

  .feature:hover {
    border-color: var(--accent);
    box-shadow: 0 0 20px var(--accent-glow);
  }

  .feature-icon {
    font-size: 1.75rem;
    display: block;
    margin-bottom: var(--space-md);
  }

  .feature h3 {
    color: var(--text-primary);
    margin-bottom: var(--space-xs);
  }

  .badge {
    font-size: 0.625rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    padding: 0.15em 0.5em;
    border: 1px solid var(--accent);
    border-radius: var(--radius-lg);
    color: var(--accent);
    vertical-align: middle;
  }

  .feature p {
    font-size: 0.875rem;
    color: var(--text-muted);
    line-height: 1.5;
  }

  .format-list {
    list-style: none;
    padding: 0;
    margin: 0;
    font-size: 0.875rem;
    line-height: 1.8;
  }

  .format-link {
    color: var(--accent);
    text-decoration: none;
    font-weight: 600;
  }

  .format-link:hover {
    color: var(--text-primary);
  }

  .format-desc {
    color: var(--text-muted);
  }

  .format-desc a {
    color: var(--text-muted);
    text-decoration: underline;
    text-underline-offset: 2px;
  }

  .format-desc a:hover {
    color: var(--accent);
  }

  .docs-link {
    display: block;
    text-align: center;
    margin-top: var(--space-xl);
    color: var(--accent);
    text-decoration: none;
    font-size: 0.875rem;
    transition: color 0.2s;
  }

  .docs-link:hover {
    color: var(--text-primary);
  }

  @media (max-width: 768px) {
    .grid {
      grid-template-columns: 1fr;
    }
  }

  @media (min-width: 769px) and (max-width: 1024px) {
    .grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }
</style>
