{
  "name": "@erode-app/agent",
  "version": "0.0.1",
  "description": "Event-driven review agent for erode architecture drift detection",
  "type": "module",
  "main": "dist/index.js",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "files": [
    "dist",
    "!dist/**/__tests__"
  ],
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "lint": "eslint . --report-unused-disable-directives --max-warnings 0",
    "lint:fix": "eslint . --report-unused-disable-directives --max-warnings 0 --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "clean": "rimraf dist",
    "prebuild": "npm run clean",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:watch": "vitest",
    "check:ci": "concurrently --group \"npm:lint\" \"npm:typecheck\" \"npm:build\""
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/erode-app/erode.git",
    "directory": "packages/agent"
  },
  "license": "MIT",
  "dependencies": {
    "@erode-app/core": "*"
  },
  "devDependencies": {
    "@erode-app/eslint-config": "*",
    "@types/node": "^24.12.2",
    "@vitest/coverage-v8": "^4.1.5",
    "concurrently": "^9.2.1",
    "eslint": "^10.3.0",
    "prettier": "^3.8.3",
    "rimraf": "^6.1.2",
    "typescript": "^6.0.3",
    "vitest": "^4.1.3"
  },
  "publishConfig": {
    "access": "public"
  },
  "engines": {
    "node": ">=24.0.0"
  }
}
